# BARUM 보장분석 통합 정본 지침 — v214-dollar2-20260725

최종 정리 2026.07.25 · 이 문서 하나가 정본이다. 이전 지침(v28·v29c·v179 등)은 **전부 폐기·대체**한다.

---

## 0. 응대·통신 규칙

- 결론 → 근거 → 실행. 단정체. 위로·감성 필러 금지. 옵션 3개 이내.
- **팩폭(사실·내 실수·한계) 먼저** → 그다음 클릭 링크.
- **가설과 확정을 분리**: 코드·파일을 직접 열어본 것만 「~이다」, 안 본 것은 「가설」이라 명시.
- **확인 없이 단정 금지.** 측정하지 않은 수치는 말하지 않는다.
- 지침 변경 가능·지점장 지시 1순위. 단 기존 정본과 다르면 **확정받고 반영**(임의 변경 금지).
- 비개발자·폰 작업 → 한 번에 한 단계, 통째 복붙 블록, 클릭 링크.
- **출력 파일명 ASCII 필수**(한글 파일명은 다운로드 링크가 깨진다).
- 매 턴 마지막에 **최종본 ZIP + 배포 안내**까지 한 번에 완결(찔끔 금지).

### 정본 관리 3원칙
1. **최종본 그대로 박기** — 지점장이 준 문구·코드를 그대로. 요약·해설 주석 금지.
2. **덮지 말고 즉시 삭제** — 바뀐 지침은 옛 내용을 남기지 말고 지운다.
3. **같은 데이터가 여러 곳에 있다 → 전수 수정** — 고친 뒤 잔재 스캔.

---

## 1. 최상위 원칙 — 4대 산출물 완전 연동

**엑셀 = 보장나무 = 보장진단서(PPT) = 보장설명서(PDF)** — 이 넷은 하나의 데이터다.
원천을 고치면 나머지 셋에 자동 반영되어야 한다. **어느 하나만 따로 손대지 않는다.**

- **등식1**: 한장보장표 = 엑셀 담보값 = PPT 담보값
- **등식2**: PPT는 완성 엑셀만 읽는다(=SUM 캐시 의존 금지)

★★ **PPT가 틀리면 원천(엑셀)이 틀린 것이다.** PPT는 엑셀 끝열을 그대로 읽으므로 PPT부터 고치려 들지 말 것.
검증법 — 엑셀 합계 **캐시값**과 진단서 PPT 텍스트를 **같은 실행에서** 대조한다.

**데이터 소스 우선순위**: ①별첨 보장현황(정본) → ②세부가입현황(교차검증) → ③한장보장표(검산)

---

## 2. 입력 엔진 — KB(3열) ↔ 롯데(2열) 구분

`sinjeong_detect(txt)`가 자동 분기한다. **두 리포트는 별개 엔진이다.**

| | **KB손보 · 메리츠 (3열)** | **롯데 let: (2열)** |
|---|---|---|
| 앵커 | 상품별 가입담보상세 / 별첨 상품별 보험가입현황 | 정상계약 리스트 |
| 열 구성 | NO \| 구분 \| **회사담보명** \| **신정원담보명** \| 가입금액 | 담보명 \| 가입금액 |
| 파서 | `parse_sinjeong` | `parse_txt` |

### KB 전용 규칙 — 롯데엔 적용 금지
1. **담보명 정본 = 회사담보명 100%.** 신정원담보명은 분류 참고용이며 담보명으로 쓰지 않는다.
   - **예외**: 회사담보명이 계약 내 중복일 때만 신정원담보명으로 분리한다.
   - 근거 — 삼성 실손 1501.4는 회사담보명이 `상해(일반상해,전체상해를 의미)` **하나로 3줄**(입원 5,000 / 처방 5 / 외래 25). 신정원명 없이는 한 칸에 뭉개진다.
   - 근거 — 삼성 New내돈내삼은 `[건강]상급종합병원 1인실 입원일당`이 **질병용·상해용 이름이 완전히 동일**. 신정원명이 유일한 구분자다.
2. **신정원담보명끼리 또 중복이면** 회사담보명에서 **병실 토큰(1인실·2~3인실)**을 뽑아 접미로 붙인다(v208).
3. **생명보험사 + 손해보험사는 '상품별 가입현황'(리포트 5~7p)을 반드시 대조**해 일치시킨다.
4. 3열에는 **잔여보험료·납입회차 열이 없다** → remain=None → 회차 폴백.

### 롯데 전용 규칙 — KB엔 적용 금지
1. **결합담보 분해**: 담보명에 '사망'+'후유장해'가 함께면 **사망 행·후유장해 행에 각각 100%**(절반 분할 아님).
   축 = '질병' 있으면 질병 / 없으면 상해. 등급 = '80%이상'·'고도' 명시 → 80% 행 / 없으면 3% 행.
   **KB·메리츠엔 결합담보가 없다.**
2. **회사명 오염**: 납입주기 칸이 채워진 행만 열이 밀려 '일시납AIG손보'가 된다 → 납입주기 토큰 제거.
3. **remain 스캔**: 보유계약 리스트 줄에서 잔여보험료·납입회차를 읽어 계약에 붙인다.

### 공통
- **규칙 하나 고치면 2열·3열 두 경로를 반드시 다 돌린다.**
- **OCR 금지.** 원본 PDF 그대로 업로드(OCR본은 KB 14계약→2계약, 메리츠 10계약→0계약으로 파괴).
- 한글이 안 나오면 '이미지'라 단정 말고 **`pdftotext` stderr의 `Missing language pack for 'Adobe-Korea1'`**부터 확인(poppler-data 문제).

---

## 3. 보장분석 제외 7종 — 이것만 제외, 그 외 전부 포함

1. 실효계약
2. 미납해지
3. 농업인안전보험(상품명에 '농업인' 표기된 것만)
4. 자동차보험 — 단 '운전자' 들어간 것(운전자보험·운전자상해)은 **포함**
5. **단체보험** — ①가입기간이 1년마다 ②상품명이 '○○단체보험' **2조건 동시 충족**해야 제외
6. **보험기간 1년 계약** — 만기−가입 = 1년(±7일, 358~372일)
7. **단기완납** — 완납(a≥b) AND **총회차 b < 60** → 제외. 일시납 제외. 회차 불명이면 포함.
   - 사유: "단기납 종신보험은 5년부터 시작한다" → **5년납(60회) 이상 완납은 포함**

★ NH농협생명/손보는 '농업인' 표기 없으면 포함. **치아보험·운전자상해보험 포함**.
★ 제외 로그 필수: `[제외6·보험기간1년]` `[제외7·단기완납]` `[제외7·기간불명]`

---

## 4. 갱신/비갱신 판정

0. **실손은 비갱신이 없다 — 무조건 갱신**(최우선). 만기9999·납입≠보장 등 어떤 조건보다 우선.
1. 담보명/상품명에 **'갱신형' 명시 → 갱신**
2. **만기 9999(종신) → 비갱신**
3. **납입기간 == 보장기간(가입~만기 연수) → 갱신** / 다르면 비갱신. **운전자도 동일**.

★★ **v207: ③번을 3열(KB·메리츠)에도 적용한다.** 구 규칙 「3열은 ③ 적용 금지」는 완전 폐기.
실측 오류 2건 — 삼성 운전자 20년납/20년만기, New내돈내삼 54년납/54년만기가 비갱신으로 박혔다.

### 폐기된 규칙(다시 쓰지 말 것)
- 삼성화재 예외(납입==보장이어도 주계약 비갱신) — 2026.07.15 삭제
- 총회차 240 초과 → 비갱신 — 2026.07.09 삭제
- 3열은 ③ 적용 금지 — 2026.07.25 삭제

---

## 5. 색 규칙

- **헤더**: 갱신=파랑 / 비갱신=빨강 / 완납=진녹, 글자 흰색
- **값 글자**: `blue = gen(계약 갱신) OR ('갱신' in 담보명raw)`. 미가입=빨강.
- **항상 파랑(강제) = 실손(입원·통원·약값) + 일상배상책임뿐**

★★ **v210: 간병인 · 간병인지원일당 · 간호통합병동의 '항상 파랑' 강제 폐기.**
이 3행은 **보험료·가입년일·만기일자·총납입기간(계약 갱신 판정) 또는 담보명 [갱신] 표기**를 따른다.
**고칠 곳 3군데** — main.py 엑셀 글자색 · main.py PPT `_silson` · coverage_benchmark 설명서.

★ 색 값 2종: **엑셀 파랑 0070C0** / **설명서·PPT 파랑 1456B0**. 엑셀 글자색이 원천 → gen_map → 설명서·PPT.

---

## 6. 합계 규칙

- 보험 최대 50건. 합계는 **항상 표 맨 끝 열**.
- **모든 합계는 단일 `=SUM(범위)`** — `=D2+E2+…` 금지(셀 복사·이동 시 참조가 어긋난다).
- **담보 행 합계** = 동적 `=SUM(C{r}:끝열{r})`. **완납 계약도 뺀다 안 뺀다 → 빼지 않는다**(보장은 살아있다).
  - 예외: 슬래시 행(1~5종·n대) / `=MIN(SUM(),80)` 자부상 / `=IF(COUNT(),0,MAX())` 간병인·간호통합병동·중입자
- **월보험료 합계** = `=SUM(C2:끝열2)`. **완납 계약은 뺀다** — 보험료 칸을 `58,340 (완납)` **텍스트**로 넣으면 SUM이 무시한다.
  - 판정 근거: **잔여보험료(완납이면 0)** 우선 → 납입횟수 a≥b 폴백 → 둘 다 불명이면 포함
- 헤더 진녹(완납) 색도 같은 `_in_sum` 근거로 통일.

---

## 7. 담보 매핑 정본

### 7.1 단독 담보 원칙 (최상위)
**허혈성 · 급성심근경색 · 뇌혈관 · 뇌졸증 · 뇌출혈** — 회사담보명이 그 담보 하나로 존재하면 **어느 회사든 해당 마스터 행에 단독 기재**. 묶음 분해 금지.

★★ **v206**: 판정 시 **접두 수식어(갱신형·비갱신형·무배당·정액·실손·[건강])를 떼고** 본다.
`갱신형 허혈성심장질환진단비`가 완전일치에 걸리지 않아 협심증+급성심근으로 분해되던 사고.

반대로 **묶음담보(특정Ⅰ·Ⅱ·Ⅲ·특정심혈관·특정15대·순환계3대)는 구성질환 행에 동일 금액 각 100%** 기재하되 **'허혈성 진단비' 행에는 절대 넣지 않는다**(I20·I24·I25는 '협심증' 행으로).

### 7.2 암
- `암진단비(유사암제외)` → **일반암**
- **`특정암`+`진단` → 고액암 행**(일반암 합산 금지). 제외어 = 주요치료·수술·일당·입원·방사선·약물·표적·양성자·세기·중입자·통원
- **유사암(갑.기.경.제) = 진단비 전용 행**(v207). 수술·치료비·일당·입원·통원·주요치료·기능항진/저하는 **[확인]큐**
  - 실측: 갑상선기능항진증치료비 100 · 유사암수술비 150이 빠져 **1,250 → 1,000**
- ★★ **유사암 자동유도 = 명시액이 하나도 없을 때만**
  - 지침 원문: 「일반암의 10% **or** 유사암이라고 적힌 금액을 **최종값으로 보고** 각각 기재」 → **둘 중 하나만** 쓴다
  - 계약 중 **유사암 명시 담보가 하나라도 있으면 그 금액이 최종값** → 나머지 계약에 ×10% 유도 **금지**
  - 명시액이 여러 개면 **각각 기재**(합산)
  - 실측 오류: 메리츠 1,000(명시) + 미래에셋 200(유도) + 라이나 100(유도) = **1,300**이 엑셀·설명서·PPT에 그대로 나갔다(정답 1,000)
- 암주요치료비 우선순위: `유사암`+`주요치료`→무시 / `암주요치료비` 명시→암주요치료비행(하이클래스보다 우선) / `하이클래스`→하이클래스(암)행

### 7.3 일당 계열
- **질병일당 / 상해일당 = 순수 담보만.** `(1-10)` `(1-20)` `(1-30)` `(1-180)`처럼 **일수 밴드로 표기된 것만** 합산(또는 각각 표기).
- ★★ **`N대질병입원일당`(2대·3대·5대…) · `특정질병입원일당`은 질병입원일당이 아니다** → **[확인]큐**
  - 실측 오류: 메리츠 `갱신형 2대질병입원일당(1일이상)` 3만이 질병일당으로 산입됐다('2대'가 제외어에 없어 통과)
- ★★ **종합병원입원일당 · 상급병원입원일당은 또 개별 행**이다. 질병일당에 넣지 않는다.
- 제외어 = 교통·암·뇌·심·허혈·간·신장·폐·위·골절·화상·특정·재해외·종합·요양·중환자·수술·**상급** + **N대**
- ★★ **'질병종합병원일당' = 그 이름으로 된 단독 담보 전용**(v209).
  **병실 등급 담보(1인실·2~3인실·2인실·3인실)는 이 행에 절대 안 넣는다** → [확인]큐
- ★★ **1인실 2행**(마스터 59·60):
  - **1인실 상급병원 = 상급종합 1인실 + 종합 1인실**(상급종합 입원 시 종합병원이하 담보도 함께 지급)
  - **1인실 종합병원 = 종합 1인실만**
  - 질병·상해는 **택일 지급이라 대표(max)**
  - 실측: 상급 20 + 종합 10 → 상급행 **30** / 종합행 **10**
  - ★ 가산은 **계약 루프 바깥에서 1회**(루프 안에 넣어 9번 중복돼 60이 나온 사고)
  - ★ **2~3인실 전용행은 없다**(만든 적 없음)
- **간호통합병동**: 담보명에 '간호간병통합서비스'(간호+간병+통합 동시). 판정을 **간병인보다 앞에** 둔다. 1-180일만.

### 7.4 운전자
- 벌금(대인)·(대인대물 미표기)→대인 / 벌금(대물)→대물 / 화재벌금=제외
- **교통사고처리지원금 = 대표(max) 1건**(합산 금지). 중상해·타인부상 등 이름만 다른 같은 담보다.
- 6주미만 진단→6주미만 / 변호사비→변호사 / 자동차부상치료비·부상위로금→자부상(14급 포함 밴드 1건)
- 최신 권장값: 벌금 대인 3천만 / 대물 500만 / 합의금 2억 / 6주미만 1천만 / 변호사 5천만 — 미달이면 재가입 권유

### 7.5 실손
- **최대 3건 보유 가능**(상해의료비 + 실손 + 상해실손). 계약별 각각 표기, **세대도 각각 판정**.
- **별첨 명시값 그대로**(구 '입원 5,000 고정·캡' 폐기)
- **세대 판정 근거 2개 = ①가입일자 ②상품코드(YYMM)**. 둘 다 있으면 **더 이른 쪽**.
- **세대 경계**: 1세대 ~2009.09 / 2세대 2009.10~2017.03(2-1 ~2012.12·2-2 ~2015.12·2-3 ~2017.03) / 3세대 ~2021.06 / **4세대 2021.07~2026.05.05** / **5세대 2026.05.06~**
  - 근거: 금융위·금감원 보도자료(2026.5.6 조간) "5월 6일부터 5세대 실손의료보험 출시·판매"
  - 상품코드 2605면 5세대, **가입일자가 있으면 일자까지**(5/1~5/5 = 4세대)
  - **3곳 통일 필수**: main.py `silson_gen` · coverage_benchmark `_gen_of` · report_weasy 표

### 7.6 수술비
- **1~5종 슬래시 표기가 정본**(상해·질병 각 1행 = 총 2행, 예 20/50/100/500/1000)
- N대(112·116·119·120대) → 대표(max) 1건
- 질병수술비 행 = '질병수술비'·'질병입원수술비' 2개만

---

## 8. 마스터 엑셀 구조

정본 = 현행 GitHub `master.xlsx`(100행·15구분). **행 삭제·이동·재정렬 금지**(추가만).

- 헤더 5행: ①고객명+회사·상품·[갱신/비갱신] ②보험료 ③가입년일 ④만기일자 ⑤총납입기간
- A열=구분 / B열=담보명 / C열~=계약별 금액(만원) / 끝열=합계
- **심장 블록 13행(38~50)**: 협심증38·심부전39·빈맥40·염증41·부정맥42·심근병증43·심장판막44·산정특례심장45·2대주요46·허혈성 진단비47·급성심근경색48·중대한급성심근49·혈전용해50
- **뇌 블록**: 뇌혈관30·뇌졸증31·중대한뇌졸증32·뇌출혈33·외상성뇌출혈34·산정특례뇌35·2대주요36·혈전용해37
- **일당 블록 51~**: 질병일당51·질병수술일당52·질병종합병원일당53·상해일당54·상해수술일당55·간병인56·간병인지원일당57·간호통합병동58·**1인실 종합병원59**·**1인실 상급병원60**·질병중환자실61·상해중환자실62
- 상해의료비 99 / 일상배상책임 100

---

## 9. 보장설명서 페이지 정본

**11** 산정특례·상한제 / **12** 재난적의료비·실손 / **13 건보 본인부담률(v203 신설)** / **14** 5세대 쉽게 / **15** 중증·비중증 / **16** 5세대 도표 / **17** 도수·체외 / **18** 수술비 / **19** 주요치료비 변천사 / **20** 10억통장 / **21~23** 재무 3장 / **24 달러보험 2탄(v214 신설)** / **25~28** 심혈관 담보 분류

**간지 목차 8항목**: 01 지원제도 11–12 / 02 5세대 실손 **13–17** / 03 수술비 18 / 04 변천사 19 / 05 10억통장 20 / 06 LIFE PLAN **21–24** / 07 비교표 부록 / 08 심혈관 **25–28**

**24p 달러보험 2탄**: 그림(달러 기준 최저임금 2018 $6.84 → 2026 $6.70)이 **위**, 표(연도별 최저시급·달러환산)가 **아래**. 한 페이지에 모음.

★ **꼬리말 총페이지가 실제보다 1 적은 것은 기존 구조**다(pgn 없는 GA 표지 1장). v213에도 동일.

★★ **페이지를 끼우면 4곳을 함께 고친다**: `.pgn` · 꼬리말 · `_fullpage(N)` · **간지 목차 `.ipg`**
★ 삽입 앵커는 **앞 페이지의 꼬리말 문자열**로 잡는다.

### 레이아웃 검증 규칙
- **잘림 안전선 y 815pt**. 본문 최하단 yMax ≤ 795 목표.
- **좌우 최대x ≤ 588**
- **div 개수를 세고 움직인다**(불균형이면 페이지 본문이 통째로 사라진다 — 실사고 2회)
- **float + 고정높이 열 금지**, 2단은 flex
- 이모지 전면금지(허용 = ★☆■□●○▲△▼▶◆※✓×→←↑↓)
- 하단 여백 10%(29.7mm) 이내면 정상 — 억지로 채우지 않는다

### 산출물 분할
'보험 인포메이션' 글자부터 끝 = PDF, 그 앞 = PPT 300dpi. **페이지 번호 하드코딩 영구금지**.
★★ **분할은 `qpdf --empty --pages src A-B -- dst` 우선**, 실패 시 pdfseparate 폴백.
구 방식은 페이지마다 이미지 리소스를 복제해 22p가 **116.3MB**(qpdf는 5.18MB).

---

## 10. 배포·검증

### 배포 8파일 — 하나라도 빠지면 그게 그날의 장애다
`main.py` · `coverage_benchmark.py` · `report_weasy.py` · `report_pptx.py` · `ga_tables.py` · `master.xlsx` · **`Dockerfile`** · `nixpacks.toml`

- **Dockerfile을 빼먹으면 PDF가 통째로 죽는다**(poppler-data 미설치 → 한글 0자)
- **qpdf가 빠지면 설명서 PDF가 116MB**가 된다
- Dockerfile은 확장자가 없어 눈에 안 띈다 → 업로드 화면에서 **8개를 세고 Commit**

### 순서
1. GitHub `bokkile83-ui/barum-bunseok-backend` → Add file → Upload files → 8개 → Commit
2. Railway(railway.com) → 4a155 서비스 → **Deploy(블루) 수동 클릭**
3. `/health` → **버전 문자열 확인**(안 바뀌면 Deploy를 안 누른 것)

### 장애 진단 순서
1. `/health` — 404면 코드가 안 올라간 것 / upstream error면 앱이 죽은 것
2. 로컬 `analyze()` 직접 호출로 재현
3. Railway 로그는 Claude가 못 본다 → 지점장이 복사
★ 코드를 되돌리기 전에 **배포 누락**부터 의심

### 검증 원칙
- **전체 파이프라인은 `analyze()` 엔드포인트를 그대로 호출**(parse_txt·build_excel만 돌린 건 검증이 아니다)
- 컨테이너에 WeasyPrint 설치 가능 → **매번 렌더해서 잰다**
- 검증용 컨테이너엔 매번 `apt-get install -y poppler-data` 필요
- **육안검수 못 했으면 "못 했다"고 명시**하고 렌더 PDF를 지점장이 보게 한다

### 제출 전 체크리스트 (하나라도 미통과 = 제출 금지)
1. 한장보장표 = 엑셀 끝열 = PPT 일치
2. 별첨 계약별 담보 전수확인 + 세부가입현황 교차
3. 헤더 5행 완전기재 + 갱신·비갱신 색
4. 끝열 담보 SUM 동적 + 월보험료 합계는 완납 제외
5. 수술비 1~5종 슬래시
6. 제외 7종 외 전부 포함
7. 렌더 육안검수(못 했으면 명시)
8. **2열·3열 두 경로 다 실행**
9. 미통과 시 자진보고

---

## 11. 확인 대기 (미해결)

1. KB '상품별 가입현황 100% 일치' 범위 — KB 37담보 vs 마스터 100행
2. 2~3인실(5+5) 최종 처리 — 현재 [확인]큐
3. 전기납 구분 기준 — `54년납/100세만기` vs `20년납/20년만기`, 현행 유지 중
4. 상해 1인실 4개(20/5/10/5) 마스터 행 신설 여부
5. 상해의료비(99행) main.py 매핑 미구현
6. requirements.txt 버전 미고정
7. 13p 병원 등급별 20/30/40/50 막대 — 14p 정본(30/40/50/60/90)과 충돌
8. 2열 경로 '타인사망교통사고처리지원금' → 교통상해사망 오분류
9. 진단서 PPT 10억통장 편집칸 미검출(가설)

---

## 부록. 앱 ↔ 주소 정본

| 앱 | 주소 | 비고 |
|---|---|---|
| **보장분석실(BARUM)** | https://web-production-4a155.up.railway.app | Railway·FastAPI |
| AI 검색포털 | https://stunning-eclair-dec9fc.netlify.app | 폴더 구조 통째 zip 필수 |
| 자료실(파랑) | https://deft-crostata-6c7312.netlify.app | PIN 1024 |
| 실손계산기(레드) | https://statuesque-figolla-0264ec.netlify.app | PIN 1009 |

★ 주소 안내 전 실제 접속 확인. Netlify 배포 = app.netlify.com/sites → Deploys → zip 드롭.
