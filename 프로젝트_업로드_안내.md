# BARUM 최종 프로젝트 업로드 안내 (2026.07.05 · v30z6)

## 1. GitHub 배포 (bokkile83-ui/barum-bunseok-backend)

배포 전 필수: GitHub 현재 main.py 1행 버전 직접 확인 → 덮기 → Railway **Deploy 버튼** → /health = `v30z6-v28align-20260705` 확인.

### 교체(덮어쓰기) 파일 3개
- `main.py`            (v30z6 — 무시4종·배증수정·삼성CI80%·성인병매핑·빈맥무행·PDF비전·앱PDF칸)
- `report_weasy.py`    (신버젼 8p — 비CI Plan B·주요치료비 변천사·상담 워크시트·빈맥 무행)
- `coverage_benchmark.py` (비CI noci 데이터)

### requirements.txt 추가 (PDF 비전용)
```
pdf2image
```
그리고 Railway 빌드에 poppler 필요: `nixpacks.toml` 또는 `apt` 패키지에 `poppler-utils` 추가.
(선택) `tesseract-ocr tesseract-ocr-kor` — 단 회전 이미지엔 무력하므로 **비전(ANTHROPIC_API_KEY)이 실질 경로**.

### 환경변수
- `ANTHROPIC_API_KEY` — PDF 비전 OCR·LLM 추출용(이미 설정돼 있으면 그대로).

## 2. 프로젝트 지식(파일) 정리

### 삭제 (구버전·충돌 유발)
- 이전 지침 전부: `지침_v30m_상세정본.md`, `지침_최종본_v30t_프로젝트용.md`, `지침_v29q.docx`, v28~v30z 지침 사본.
- 구 report_weasy(5페이지·빈맥 포함) 사본.
- 구 산출물 pdf/xlsx(빈맥 포함·5p 설명지) 표본.

### 추가
- `지침_정본_v30z6.md`  ← 유일 정본(이 파일 하나만 유지).
- `main.py` · `report_weasy.py` · `coverage_benchmark.py` (v30z6).
- 표본 산출물: `보장설명지_주정은.pdf`(CI 80% 8p) · `보장설명지_양예서.pdf`(비CI Plan B 8p).

## 3. 오늘(07.05) 확정 요약
- 무시 4종(전이암·표준금액·고액항암) 완전 드롭 / 통합전이암=개별 대표1개.
- 뇌·심 진단비Ⅰ·Ⅱ 배증버그 수정(dedup 일치).
- 삼성 리빙케어 CI = 선지급 80%형 자동판별.
- 성인병진단금 = 급성심근(100%) + 뇌졸증/뇌출혈(세부가입) 매핑([확인] HOLD 폐기).
- 빈맥 무행(v28 정본) — 엑셀·PPT·설명서 전부 정합.
- PDF(이미지·회전) 비전 입력 + txt/PDF 동시 업로드(OR).
- 보장설명지 신버젼 8p — CI/비CI 자동분기 + 변천사 + 워크시트.

## 4. 배포 후 필수 검증 (미검증 항목)
- **PDF 비전 실추출 품질**: 배포 후 주정은.pdf 1건 업로드 → 삼성 리빙케어 3,000/2,400가 깨끗이 나오는지 실측(샌드박스엔 API키 없어 미검증).
- txt+PDF 동시 업로드 시 `source` 표기·제목·깨짐 교차확인.
