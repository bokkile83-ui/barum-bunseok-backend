# PPT 디자인 원복 — 파일 1개

## 이 파일
`원복/report_pptx.py`  ·  1,985B  ·  49줄
= GitHub 커밋 `f106c7ca` 원본 그대로 (2026.07.07 이미지 방식)

## 올리는 법
1. https://github.com/bokkile83-ui/barum-bunseok-backend/upload/main
2. `report_pptx.py` 를 창에 끌어다 놓기
3. 맨 아래 초록 `Commit changes`
   (같은 이름이라 자동으로 덮어쓴다)

## 그다음 반드시
https://railway.app/dashboard → `Deployments` → 맨 위가 초록 `Active` 인지 확인.
안 되면 우측 `Deploy` 버튼.

## 결과
- PPT 디자인 → 2026.07.07 원래대로 (A4 세로)
- 선지급률 80% → 유지됨 (coverage_benchmark.py 라 무관)
- PPT 편집 → 다시 불가 (그림)

---

# 파일 안 받아져도 되는 방법 — Railway 롤백

https://railway.app/dashboard → 프로젝트 → `Deployments`

목록:
```
Update report_pptx.py     ← 05:21  디자인 바뀐 시점
Add files via upload      ← 05:06  ★ 이 줄
Add files via upload      ← 04:58
```
`05:06` 줄 우측 점 세 개 `⋯` → `Rollback`

파일도 붙여넣기도 필요 없다. 선지급률 80%는 살아 있다.

---

# 오늘 배포 완료된 것
- 선지급률 39% → **80%**  (coverage_benchmark.py)
- CI 3-state 정상 (main.py) — 김진구님 `ci` / 이종수·김규훈 `none`
- 보장설명지 PDF 정상

# 아직 안 된 것 (급하지 않다)
- PDF 7p 암 카드에 `+ci 3,200만` 병기 → report_weasy.py
- main.py 한 줄 → 급성심근경색 4,200 반영
